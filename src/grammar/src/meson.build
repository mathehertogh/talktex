libgrammar_files += files('cpp/grammar.cpp', 'cpp/visitor/syntaxvisitor.h')
# TODO: add once we have something in these files
# libgrammar_files += flex_gen.process('flex_bison/compiler.l')
# libgrammar_files += bison_gen.process('flex_bison/compiler.y')